/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at traversing the DOM using JavaScript.
 */

const frogBack = document.querySelector("#pack02");
const frogAge = frogBack.querySelectorAll(".backpack__pockets")[0];

console.log(frogBack.querySelector("h1"));
console.log(frogAge);
