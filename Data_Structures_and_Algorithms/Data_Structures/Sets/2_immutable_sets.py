# Immutable - Frozen Set
primary_colors = frozenset(['red', 'blue', 'yellow'])

if 'blue' in primary_colors:
    print('Blue is in primary colors!')
else:
    print('Blue is not in primary colors!')

if 'green' in primary_colors:
    print('Green is in primary colors!')
else:
    print('Green is not in primary colors!')
